<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simayodika.inventaris">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.simayodika.inventaris.ReportActivity">
        </activity>
        <activity android:name="com.example.simayodika.inventaris.views.peminjaman.FormActivity" />
        <activity
            android:name="com.example.simayodika.inventaris.views.pegawai.PegawaiActivity"
            android:label="Pegawai" />
        <activity android:name="com.example.simayodika.inventaris.views.LoginPegawaiActivity" />
        <activity android:name="com.example.simayodika.inventaris.views.LoginPengelolaActivity" />
        <activity android:name="com.example.simayodika.inventaris.views.peminjaman.PeminjamanActivity" />
        <activity
            android:name="com.example.simayodika.inventaris.views.operator.OperatorActivity"
            android:label="Operator" />
        <activity android:name="com.example.simayodika.inventaris.views.admin.DetailInventarisActivity" />
        <activity android:name="com.example.simayodika.inventaris.views.admin.AddInventarisActivity" />
        <activity
            android:name="com.example.simayodika.inventaris.views.admin.AdminActivity"
            android:label="Admin" />
        <activity android:name="com.example.simayodika.inventaris.views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>